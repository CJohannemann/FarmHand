export default function App() {
  return (
    <main className="shell">
      <h1>FarmHand</h1>
      <p className="tagline">Farm and homestead records.</p>
      <p className="status">
        Scaffold only — no features yet. The toolchain builds and runs.
      </p>
    </main>
  )
}
