# Trimmed self-hosted backend for FarmHand: just the three pieces the app
# actually uses (Postgres, its REST API, and Auth) — not Supabase's full
# stack (no Realtime, Storage, Studio, or Kong). nginx on the host does the
# job Kong would, routing /rest/v1 and /auth/v1 to the right container; see
# nginx-farmhand-api.conf.
#
# Uses the official supabase/postgres image rather than plain postgres —
# it already bootstraps the auth schema, roles (anon/authenticated/
# service_role/authenticator/supabase_auth_admin), and the auth.uid() /
# auth.role() helper functions FarmHand's row-level security policies
# depend on. Hand-rolling that ourselves is exactly the kind of thing that
# goes subtly, securely wrong.
#
# Nothing here is reachable from outside the VPS directly — only nginx
# (already running on the host) is, and it proxies in over 127.0.0.1.

services:
  db:
    image: supabase/postgres:15.1.1.78
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXP: 3600
    volumes:
      - ./data/db:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"

  rest:
    image: postgrest/postgrest:v12.2.3
    restart: unless-stopped
    depends_on:
      - db
    environment:
      # libpq looks up the container's own numeric user id via getpwuid() to
      # find a home directory (for .pgpass / SSL cert paths) when HOME isn't
      # set — this image has no matching /etc/passwd entry for it, so that
      # lookup fails outright with "could not look up local user ID ...".
      # Setting HOME explicitly skips the lookup entirely.
      HOME: /tmp
      PGRST_DB_URI: postgres://authenticator:${POSTGRES_PASSWORD}@db:5432/postgres
      PGRST_DB_SCHEMAS: public
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: ${JWT_SECRET}
      PGRST_DB_USE_LEGACY_GUCS: "false"
    ports:
      # 3001 collides with the baseball site's own backend on this VPS.
      - "127.0.0.1:8001:3000"

  auth:
    image: supabase/gotrue:v2.170.0
    restart: unless-stopped
    depends_on:
      - db
    environment:
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      API_EXTERNAL_URL: https://${API_DOMAIN}
      GOTRUE_SITE_URL: https://${FRONTEND_DOMAIN}
      GOTRUE_URI_ALLOW_LIST: https://${FRONTEND_DOMAIN}/**
      GOTRUE_DB_DRIVER: postgres
      DATABASE_URL: postgres://supabase_auth_admin:${POSTGRES_PASSWORD}@db:5432/postgres
      GOTRUE_JWT_SECRET: ${JWT_SECRET}
      GOTRUE_JWT_EXP: 3600
      # Despite the startup deprecation notice, this is still what sets a
      # new user's JWT "role" claim in this version — remove it and every
      # signup gets role:"" instead of "authenticated", which PostgREST
      # then can't SET ROLE to (confirmed by testing, not documentation).
      GOTRUE_JWT_DEFAULT_GROUP_NAME: authenticated
      GOTRUE_DISABLE_SIGNUP: "false"
      GOTRUE_EXTERNAL_EMAIL_ENABLED: "true"
      # Was "true" while SMTP was unproven, so signup didn't depend on mail.
      # That is not safe to leave on for a public signup page: it hands out a
      # working account for an address nobody has proven they own, so a
      # typo'd email becomes an account a stranger can take over via password
      # reset, and nothing stops a bot opening farms until the disk fills.
      # SMTP is confirmed delivering (a real reset email arrived via Brevo),
      # so this is now off and new accounts must confirm.
      #
      # BEFORE APPLYING THIS ON THE LIVE SERVER: the confirmation link points
      # at the bare /verify path, which needs the `location = /verify` block
      # in nginx-farmhand-api.conf. That block is in this repo but was never
      # copied to the live nginx (certbot rewrote that file in place — see
      # commit 55af2f5). Turn this off before /verify works and every
      # confirmation email 404s, which breaks signup completely. Apply the
      # nginx block first, send yourself a test signup, then restart auth.
      GOTRUE_MAILER_AUTOCONFIRM: "false"
      GOTRUE_SMTP_HOST: ${SMTP_HOST}
      GOTRUE_SMTP_PORT: ${SMTP_PORT}
      GOTRUE_SMTP_USER: ${SMTP_USER}
      GOTRUE_SMTP_PASS: ${SMTP_PASS}
      GOTRUE_SMTP_SENDER_NAME: Farmhand Management
      GOTRUE_SMTP_ADMIN_EMAIL: ${SMTP_ADMIN_EMAIL}
    ports:
      - "127.0.0.1:9999:9999"
